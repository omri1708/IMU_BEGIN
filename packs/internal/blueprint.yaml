name: internal
services:
  - name: internal_core
    endpoints:
      - POST /entities
      - GET  /entities
events:
  - entity.created
  - entity.updated
db:
  - table: entities
    columns: [id:int:pk, name:str, description:text]
ux:
  - screen: Entities
    route: /entities
iac:
  - kustomize: overlays/dev
  - helm: charts/imu

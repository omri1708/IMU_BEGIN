receivers:
  otlp:
    protocols:
      http: {}
      grpc: {}
processors:
  batch: {}
exporters:
  debug: {}   # מחליף את logging
service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
